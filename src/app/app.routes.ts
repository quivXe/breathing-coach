import { Routes } from '@angular/router';
import { BreathingComponent } from './features/breathing/breathing.component';

export const routes: Routes = [
  { path: '', component: BreathingComponent },
];
